Rejector for macOS
==================

1. Drag Rejector.app to the Applications folder.
2. If macOS prevents Rejector from opening, open Terminal and run:

   xattr -cr /Applications/Rejector.app

3. Open Rejector from Applications.

If you installed Rejector somewhere else, replace /Applications/Rejector.app
with the path to that copy of the application.
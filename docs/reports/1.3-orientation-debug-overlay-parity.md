# Task 1.3: Orientation Debug Overlay Detail Parity

## 1. Objective
Render the WPF orientation debug stars, triangle, flip state, and status from authoritative Core analysis data.

## 2. Task Contract
- Task ID: 1.3
- Epic: Preview parity
- User behavior to match: Enabling Orientation debug shows numbered orientation stars, the selected triangle, and the orientation decision summary.
- Baseline references:
  - src/PreviewWindow.xaml.cs: UpdateOrientationDebugOverlay
  - src/Rejector.Core/Services/RustafitsService.cs: AnalyzeOrientation
- Target files expected:
  - src/Rejector.Avalonia/ViewModels/MainWindowViewModel.cs
  - src/Rejector.Avalonia/Views/FramePreviewWindow.cs
- Acceptance criteria:
  - First frame carries reference orientation debug data.
  - Later frames carry candidate orientation debug data from Core.
  - Numbered markers use authoritative orientation stars.
  - Triangle uses Core TriangleIndices, not arbitrary measured stars.
  - Summary reports flipped/not flipped, star count, and Core status text.

## 3. Baseline Extraction Notes
WPF renders all `OrientationDebugInfo.Stars`, labels them from 1, connects exactly the three `TriangleIndices`, and shows `Rotate180`, star count, and `StatusText` in a top-left summary.

## 4. Changes Made
- File: src/Rejector.Avalonia/ViewModels/MainWindowViewModel.cs
- What changed: Computes reference/candidate orientation debug data during analysis and carries it through result context and frame summary.
- File: src/Rejector.Avalonia/Views/FramePreviewWindow.cs
- What changed: Replaced the invented top-three-star triangle with WPF-equivalent numbered markers, Core triangle indices, and orientation summary.

## 5. Validation
Commands run:
```bash
dotnet build src/Rejector.Avalonia/Rejector.Avalonia.csproj
scripts/verify-parity.sh
```

Outcomes:
- Build Avalonia: Passed.
- Build Solution: Passed.
- Core Tests: 6 passed, 0 failed.

## 6. Visual Evidence
- New screenshot(s): src/shots/1.3-orientation-debug-current.png
- Legacy reference screenshot(s): Pending.
- Comparison notes: The second of two real `LIGHT/R` frames displays 12 numbered authoritative orientation stars and the Core summary `not flipped`, score 0.878. Core returned no three-index triangle for this candidate, so the triangle is correctly omitted under the WPF rule.

## 7. Acceptance Matrix
- Behavior matched: Yes
- Build passed: Yes
- Tests passed: Yes
- Screenshot captured: Yes
- Remaining delta listed: Yes

## 8. Remaining Deltas
No known implementation delta. A future regression capture should include a candidate where Core returns exactly three TriangleIndices and a confirmed 180-degree flip.

## 9. Risks and Follow-Ups
- Risk: Orientation analysis adds work during initial frame processing.
- Follow-up task: Task 1.4 curvature overlay detail parity.

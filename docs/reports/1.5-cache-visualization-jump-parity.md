# Task 1.5: Cache Visualization and Jump Parity

## 1. Objective
Match the WPF score strip, cache dots, active marker, rejected strike, and direct frame navigation.

## 2. Task Contract
- Task ID: 1.5
- Epic: Preview parity
- User behavior to match: The preview side strip visualizes every visible frame and supports direct navigation.
- Baseline references:
  - src/PreviewWindow.xaml.cs: RedrawCacheIndicators
- Target files expected:
  - src/Rejector.Avalonia/Views/FramePreviewWindow.cs
- Acceptance criteria:
  - Scores normalize across visible positive scores and use the WPF red/yellow/green ramp.
  - Rejected frames have a white strike.
  - Current frame has the yellow active triangle.
  - Cached frames have a muted blue dot.
  - Clicking a marker navigates directly to that frame.

## 3. Baseline Extraction Notes
WPF draws 2-6 px score markers distributed over the available height, normalizes positive scores, places the current triangle to the left, places cache dots on the right, and overlays a transparent frame-jump target.

## 4. Changes Made
- File: src/Rejector.Avalonia/Views/FramePreviewWindow.cs
- What changed: Replaced fixed score thresholds and active outlines with normalized WPF colors, reject strikes, active triangles, cache dots, and retained direct click navigation.

## 5. Validation
Commands run:
```bash
dotnet build src/Rejector.Avalonia/Rejector.Avalonia.csproj
scripts/verify-parity.sh
```

Outcomes:
- Build Avalonia: Passed.
- Build Solution: Passed.
- Core Tests: 7 passed, 0 failed.

## 6. Visual Evidence
- New screenshot(s): src/shots/1.5-cache-navigation-current.png
- Legacy reference screenshot(s): Pending.
- Comparison notes: Three real `LIGHT/R` frames show three score markers, reject strikes, the frame-1 active triangle, and a cache dot for the loaded preview. The legacy Right binding advanced the visible counter from 1/3 to 2/3.

## 7. Acceptance Matrix
- Behavior matched: Yes
- Build passed: Yes
- Tests passed: Yes
- Screenshot captured: Yes
- Remaining delta listed: No

## 8. Remaining Deltas
None.

## 9. Risks and Follow-Ups
- Risk: Very large visible frame sets compress markers to the two-pixel minimum, matching the WPF behavior.
- Follow-up task: Complete Task 1.0 manual pan validation on Windows and macOS.

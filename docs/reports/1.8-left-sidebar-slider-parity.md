# Task 1.8: Left Sidebar Slider Controls Parity

## 1. Objective
Restore slider controls for threshold values in the Avalonia left sidebar, matching WPF interaction style.

## 2. Task Contract
- Task ID: 1.8
- Epic: Preview parity
- User behavior to match: Left sidebar values can be adjusted via sliders (not only numeric boxes).
- Baseline references:
  - src/PreviewWindow.xaml: threshold control groups
  - src/Views/SettingsOverlayView.xaml: MetricSliderControl usage
- Target files expected:
  - src/Rejector.Avalonia/Views/MainWindow.axaml
- Acceptance criteria:
  - Max FWHM, Max HFR, Max Ecc, Max Background, Min Stars, Min Trail each include a slider.
  - Existing numeric entry remains available and synchronized.

## 3. Baseline Extraction Notes
WPF uses slider-centric value adjustment for the same metrics, with numeric readout/edit controls bound to the same VM values.

## 4. Changes Made
- File: src/Rejector.Avalonia/Views/MainWindow.axaml
- What changed: Expanded the automatic rejection grid to include bound sliders alongside numeric controls for the six threshold metrics.

## 5. Validation
Commands run:
```bash
dotnet build src/Rejector.Avalonia/Rejector.Avalonia.csproj
scripts/verify-parity.sh
```

Outcomes:
- Build Avalonia: Pass (`dotnet build src/Rejector.Avalonia/Rejector.Avalonia.csproj`).
- Build Solution: Pass (`scripts/verify-parity.sh` step 2).
- Core Tests: Pass (`scripts/verify-parity.sh` step 3, 8 passed).

## 6. Visual Evidence
- New screenshot(s):
  - src/shots/1.8-left-sidebar-sliders-current.png
- Legacy reference screenshot(s): Available via WPF baseline parity references.
- Comparison notes: Left sidebar threshold groups show slider-plus-numeric synchronized controls matching intended WPF interaction pattern.

## 7. Acceptance Matrix
- Behavior matched: Yes
- Build passed: Yes
- Tests passed: Yes
- Screenshot captured: Yes
- Remaining delta listed: Yes

## 8. Remaining Deltas
No remaining functional gap in Task 1.8 scope. Any further work would be optional slider UX refinement.

## 9. Risks and Follow-Ups
- Risk: Wide slider ranges (background, stars) can reduce precision at small drag increments.
- Follow-up task: None.

## 10. Final Status
- Task 1.8 is complete.
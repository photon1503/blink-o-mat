# Task 1.2: Loupe / Pixel Inspector Parity

## 1. Objective
Port the WPF right-button loupe and live pixel readout to the Avalonia preview.

## 2. Task Contract
- Task ID: 1.2
- Epic: Preview parity
- User behavior to match: Hold the right mouse button over the preview to inspect a magnified grayscale sample and live pixel statistics; hide it on release or image change.
- Baseline references:
  - src/PreviewWindow.xaml: LoupeCanvas and LoupeBorder
  - src/PreviewWindow.xaml.cs: ShowLoupeAt
  - src/PreviewWindow.xaml.cs: BuildLoupeBitmap
  - src/PreviewWindow.xaml.cs: PreviewImage_MouseRightButtonDown/Up
- Target files expected:
  - src/Rejector.Avalonia/Views/FramePreviewWindow.cs
  - src/Rejector.Avalonia/ViewModels/MainWindowViewModel.cs
- Acceptance criteria:
  - Right-button press shows the loupe and captures the pointer.
  - Movement while held updates the loupe.
  - Release or image change hides the loupe.
  - Loupe samples 31 x 31 pixels and uses nearest-neighbor display.
  - Readout formatting matches X, Y, K, Min, Max, and Mean labels.
  - The 204 x 140 loupe remains inside the preview viewport.

## 3. Baseline Extraction Notes
WPF samples a bounded 31 x 31 Gray8 crop centered on the rounded image pixel coordinate, enlarges it with nearest-neighbor, reports center/min/max/mean byte values, offsets the panel 16 pixels from the pointer, and clamps it to the overlay canvas.

## 4. Changes Made
- File: src/Rejector.Avalonia/ViewModels/MainWindowViewModel.cs
- What changed: Retains the already-rendered preview RGB buffer and exposes bounded 31 x 31 grayscale sampling without disk access or full-frame copies per pointer move.
- File: src/Rejector.Avalonia/Views/FramePreviewWindow.cs
- What changed: Added the WPF-sized loupe, readout labels, nearest-neighbor crop display, right-button capture/move/release lifecycle, and viewport-clamped placement.

## 5. Validation
Commands run:
```bash
dotnet build src/Rejector.Avalonia/Rejector.Avalonia.csproj
scripts/verify-parity.sh
```

Outcomes:
- Build Avalonia: Passed.
- Build Solution: Passed.
- Core Tests: 6 passed, 0 failed.

## 6. Visual Evidence
- New screenshot(s): src/shots/1.2-loupe-pixel-inspector-current.png
- Legacy reference screenshot(s): Pending.
- Comparison notes: A real `LIGHT/R` frame shows the 204 x 140 loupe, nearest-neighbor 31 x 31 grayscale crop, X/Y/K/Min/Max/Mean labels, and viewport-clamped placement.

## 7. Acceptance Matrix
- Behavior matched: Yes
- Build passed: Yes
- Tests passed: Yes
- Screenshot captured: Yes
- Remaining delta listed: Yes

## 8. Remaining Deltas
No known implementation delta. Right-button hold/move/release remains a recommended manual cross-platform regression check because macOS Accessibility blocks synthetic pointer input.

## 9. Risks and Follow-Ups
- Risk: macOS synthetic pointer input remains unavailable without Accessibility permission.
- Follow-up task: Task 1.3 orientation debug overlay detail parity.
